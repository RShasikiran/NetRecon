# NetRecon Requirements
# All core dependencies are pre-installed on Kali Linux
# Install additional Python packages with:
# pip3 install -r requirements.txt --break-system-packages

python-whois>=0.8.0
dnspython>=2.3.0
requests>=2.28.0

# Optional: for PDF export
# wkhtmltopdf is installed via apt: sudo apt install wkhtmltopdf

# Optional: for web screenshots
# selenium>=4.0.0
# sudo apt install chromium-driver
